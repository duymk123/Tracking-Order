spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/tracking_order


feature-flag:
  service:
    url: http://localhost:8081
  sync-token: change-me


logging:
  level:
    com:
      example:
        trackingorder: DEBUG
    org:
      springframework:
        security: DEBUG
      hibernate:
        SQL: DEBUG
